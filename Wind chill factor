#include <stdio.h>
#include <math.h>

int main() {
  float t, v, WCF;

  scanf("%f", &t);
  scanf("%f", &v);

  WCF = 35.74 + 0.6215 * t + (0.4275 * t - 35.75) * pow(v, 0.16);

  printf("%.2f", WCF);

  return 0;
}
